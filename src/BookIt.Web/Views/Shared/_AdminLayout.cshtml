<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - BookIt Admin</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @await RenderSectionAsync("Styles", required: false)
    <script>
        // Apply saved theme immediately to prevent flash of wrong theme
        (function(){ var t=localStorage.getItem('bookit-theme')||'light'; document.documentElement.setAttribute('data-theme',t); })();
    </script>
    <style>
        :root {
            --primary: @(ViewBag.PrimaryColor ?? "#6c5ce7");
            --primary-dark: @(ViewBag.PrimaryColor ?? "#5549d1");
        }
    </style>
</head>
<body>
<div class="admin-wrapper">
    <!-- Sidebar overlay (mobile) -->
    <div class="sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>

    <!-- Sidebar -->
    <aside class="admin-sidebar" id="adminSidebar">
        <!-- Brand -->
        <a href="/" class="sidebar-brand text-decoration-none">
            <div class="sidebar-brand-icon">
                <i class="bi bi-calendar-check"></i>
            </div>
            <div>
                <span class="sidebar-brand-text">BookIt</span>
                <span class="sidebar-brand-sub">Admin Portal</span>
            </div>
        </a>

        <!-- Navigation -->
        <nav class="sidebar-nav">
            @{
                var slug = Context.Session.GetString("TenantSlug") ?? "";
                var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
                var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
            }
            <div class="sidebar-section-title">Main Menu</div>

            <a href="/@slug/admin" class="sidebar-link @(currentAction == "Index" && currentController == "Admin" ? "active" : "")">
                <i class="bi bi-speedometer2"></i>
                <span>Dashboard</span>
            </a>
            <a href="/@slug/admin/Calendar" class="sidebar-link @(currentController == "Admin" && currentAction == "Calendar" ? "active" : "")">
                <i class="bi bi-calendar3"></i>
                <span>Calendar</span>
            </a>

            <div class="sidebar-section-title">Management</div>

            <a href="/@slug/admin/Services" class="sidebar-link @(currentController == "Admin" && currentAction == "Services" ? "active" : "")">
                <i class="bi bi-grid-3x3-gap"></i>
                <span>Services</span>
            </a>
            <a href="/@slug/admin/Staff" class="sidebar-link @(currentController == "Admin" && currentAction == "Staff" ? "active" : "")">
                <i class="bi bi-people"></i>
                <span>Staff</span>
            </a>
            <a href="/@slug/admin/Classes" class="sidebar-link @(currentController == "Admin" && currentAction == "Classes" ? "active" : "")">
                <i class="bi bi-calendar3-event"></i>
                <span>Classes</span>
            </a>
            <a href="/@slug/admin/Customers" class="sidebar-link @(currentController == "Admin" && currentAction == "Customers" ? "active" : "")">
                <i class="bi bi-person-lines-fill"></i>
                <span>Customers</span>
            </a>
            <a href="/@slug/admin/Forms" class="sidebar-link @(currentController == "Admin" && (currentAction == "Forms" || currentAction == "FormBuilder") ? "active" : "")">
                <i class="bi bi-ui-checks"></i>
                <span>Forms</span>
            </a>
            <a href="/@slug/admin/Interviews" class="sidebar-link @(currentController == "Admin" && currentAction == "Interviews" ? "active" : "")">
                <i class="bi bi-person-video3"></i>
                <span>Interviews</span>
            </a>
            <a href="/@slug/book" target="_blank" class="sidebar-link">
                <i class="bi bi-box-arrow-up-right"></i>
                <span>Booking Page</span>
            </a>

            <div class="sidebar-section-title">Configuration</div>

            <a href="/@slug/admin/Settings" class="sidebar-link @(currentController == "Admin" && currentAction == "Settings" ? "active" : "")">
                <i class="bi bi-gear"></i>
                <span>Settings</span>
            </a>
            <a href="/@slug/admin/Subscriptions" class="sidebar-link @(currentController == "Admin" && currentAction == "Subscriptions" ? "active" : "")">
                <i class="bi bi-credit-card-2-front"></i>
                <span>Subscription</span>
            </a>
        </nav>

        <!-- User Footer -->
        <div class="sidebar-footer">
            @{
                var userName = Context.Session.GetString("UserName") ?? "Admin";
                var initials = string.Concat(userName.Split(' ').Take(2).Select(p => p.Length > 0 ? p[0].ToString() : "")).ToUpper();
                if (initials.Length == 0) initials = "A";
            }
            <div class="sidebar-user mb-3">
                <div class="sidebar-avatar">@initials</div>
                <div class="sidebar-user-info">
                    <div class="sidebar-user-name">@userName</div>
                    <div class="sidebar-user-role">Administrator</div>
                </div>
            </div>
            <a href="/Account/Logout" class="sidebar-link" style="padding-left:0; border:none;">
                <i class="bi bi-box-arrow-left"></i>
                <span>Sign Out</span>
            </a>
        </div>
    </aside>

    <!-- Main Content -->
    <div class="admin-main">
        <!-- Topbar -->
        <header class="admin-topbar">
            <button class="btn btn-sm d-lg-none" onclick="toggleSidebar()" aria-label="Toggle sidebar">
                <i class="bi bi-list fs-5"></i>
            </button>
            <div class="flex-grow-1 d-flex align-items-center gap-2">
                <nav aria-label="breadcrumb" class="d-none d-md-block">
                    <ol class="breadcrumb mb-0 small">
                        <li class="breadcrumb-item"><a href="/@slug/admin" class="text-decoration-none text-muted">Dashboard</a></li>
                        @if (ViewData["Title"] != null && ViewData["Title"]?.ToString() != "Dashboard")
                        {
                            <li class="breadcrumb-item active" aria-current="page">@ViewData["Title"]</li>
                        }
                    </ol>
                </nav>
            </div>
            <div class="d-flex align-items-center gap-2">
                <a href="/@slug/book" target="_blank" class="btn btn-sm btn-outline-secondary d-none d-md-inline-flex align-items-center gap-1">
                    <i class="bi bi-eye"></i><span>View Site</span>
                </a>
                <button class="dark-mode-toggle" onclick="toggleDarkMode()" aria-label="Toggle dark mode" title="Toggle dark/light mode">
                    <i class="dark-mode-icon bi bi-moon-fill"></i>
                </button>
                <div class="dropdown">
                    <button class="nav-profile-btn" data-bs-toggle="dropdown" aria-expanded="false" aria-label="Profile menu">
                        <div class="nav-avatar" style="--avatar-size:32px;--avatar-font:.8rem;">@initials</div>
                        <span class="nav-username d-none d-md-inline">@userName</span>
                        <i class="bi bi-chevron-down nav-chevron"></i>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end nav-profile-dropdown shadow border-0 mt-2" style="min-width:220px;">
                        <li>
                            <div class="dropdown-header-user px-3 py-2">
                                <div class="nav-avatar nav-avatar-lg">@initials</div>
                                <div>
                                    <div class="fw-semibold" style="font-size:.875rem;">@userName</div>
                                    <div class="text-muted" style="font-size:.75rem;">TenantAdmin · @slug</div>
                                </div>
                            </div>
                        </li>
                        <li><hr class="dropdown-divider my-1"></li>
                        <li><a class="dropdown-item" href="/@slug/admin"><i class="bi bi-speedometer2 me-2 text-brand"></i>Dashboard</a></li>
                        <li><a class="dropdown-item" href="/@slug/admin/Settings"><i class="bi bi-person-gear me-2 text-muted"></i>Profile &amp; Settings</a></li>
                        <li><a class="dropdown-item" href="/@slug/book" target="_blank"><i class="bi bi-box-arrow-up-right me-2 text-muted"></i>View Booking Page</a></li>
                        <li><hr class="dropdown-divider my-1"></li>
                        <li><a class="dropdown-item text-danger" href="/Account/Logout"><i class="bi bi-box-arrow-right me-2"></i>Sign Out</a></li>
                    </ul>
                </div>
            </div>
        </header>

        <!-- Flash messages -->
        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show m-4 mb-0 rounded-3 border-0 shadow-sm" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>@TempData["Success"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show m-4 mb-0 rounded-3 border-0 shadow-sm" role="alert">
                <i class="bi bi-exclamation-circle-fill me-2"></i>@TempData["Error"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <main class="admin-content">
            @RenderBody()
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
<script>
function toggleSidebar() {
    document.getElementById('adminSidebar').classList.toggle('show');
    document.getElementById('sidebarOverlay').classList.toggle('show');
}
function closeSidebar() {
    document.getElementById('adminSidebar').classList.remove('show');
    document.getElementById('sidebarOverlay').classList.remove('show');
}
</script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>
