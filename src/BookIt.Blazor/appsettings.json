{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiSettings": {
    "BaseUrl": "http://localhost:5248"
  },
  "SubdomainConfig": {
    // Set to your root domain, e.g. "bookit.com", to enable wildcard subdomain routing.
    // Tenants will then be accessible at {slug}.bookit.com in addition to /{slug}/book.
    "BaseDomain": ""
  },
  "Sentry": {
    "Dsn": "",
    "TracesSampleRate": 0.1
  }
}
